---
title: "Chat"
description: "对话补全接口"
---

# Chat 对话补全

OpenAI 兼容的对话补全接口，支持文本、视觉、工具调用、流式响应和推理模型。

<Card title="Endpoints" icon="code">
  - [POST /v1/chat/completions](/api-reference/chat/create-chat-completion) — 创建对话补全
  - [POST /v1/responses](/api-reference/chat/create-response) — OpenAI Responses 格式
</Card>

<Note>
  本页面为中文版。完整的接口细节、Schema 定义、示例请参考
  [English API Reference](/api-reference/chat)。OpenAPI 规范
  是英文，Mintlify 自动渲染的接口详情页在英文版。
</Note>

## 支持的模型

`gpt-5`、`gpt-4o`、`claude-opus-4-7`、`claude-sonnet-4-6`、`gemini-2.5-pro` 等。

详见 [GET /v1/models](/api-reference/models/list-models)。

## 流式响应

设置 `stream: true` 接收 Server-Sent Events。详见 [流式指南](/guides/streaming)。

## 中文入门示例

```bash
curl https://api.infinityblue.com/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "用一句话介绍你自己"}]
  }'
```

{
  "name": "infinityblue-api-docs",
  "version": "0.1.0",
  "description": "InfinityBlue API documentation — OpenAPI spec + Mintlify site",
  "private": true,
  "scripts": {
    "lint": "redocly lint openapi/openapi.zh.yaml openapi/openapi.en.yaml",
    "validate:zh": "mint validate openapi/openapi.zh.yaml",
    "validate:en": "mint validate openapi/openapi.en.yaml",
    "validate": "npm run validate:zh && npm run validate:en",
    "dev": "mint dev"
  },
  "devDependencies": {
    "@redocly/cli": "^1.25.0",
    "mint": "^4.0.0"
  }
}
